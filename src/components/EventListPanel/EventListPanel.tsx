import React from 'react';
import PropTypes from 'prop-types';
//import { Test } from './EventListPanel.styles';

const EventListPanel = (props) => (
  <div className="EventListPanelWrapper">
    Test content
  </div>
);

EventListPanel.propTypes = {
  // bla: PropTypes.string,
};

EventListPanel.defaultProps = {
  // bla: 'test',
};

export default EventListPanel;
